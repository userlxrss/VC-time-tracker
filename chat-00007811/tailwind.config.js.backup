/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './src/**/*.{js,ts,jsx,tsx,mdx}',
    './examples/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  darkMode: 'class',
  theme: {
    extend: {
      // === Refined Dark Mode Color Palette ===
      colors: {
        // Background colors - Clear hierarchy
        bg: {
          primary: 'var(--color-bg-primary)',
          secondary: 'var(--color-bg-secondary)',
          elevated: 'var(--color-bg-elevated)',
          interactive: 'var(--color-bg-interactive)',
        },

        // Surface colors for different UI elements
        surface: {
          elevated: 'var(--color-surface-elevated)',
          overlay: 'var(--color-surface-overlay)',
          border: 'var(--color-surface-border)',
        },

        // Layout-specific colors
        layout: {
          sidebar: 'var(--color-sidebar-bg)',
          header: 'var(--color-header-bg)',
          content: 'var(--color-content-bg)',
        },

        // Text colors - Clear hierarchy
        text: {
          primary: 'var(--color-text-primary)',
          secondary: 'var(--color-text-secondary)',
          tertiary: 'var(--color-text-tertiary)',
          inverse: 'var(--color-text-inverse)',
        },

        // Accent colors - Blue with glow
        accent: {
          primary: 'var(--color-accent-primary)',
          hover: 'var(--color-accent-hover)',
          glow: 'var(--color-accent-glow)',
        },

        // Border colors - Subtle but clear
        border: {
          light: 'var(--color-border-light)',
          medium: 'var(--color-border-medium)',
          strong: 'var(--color-border-strong)',
        },

        // Shadow colors - Deep and rich
        shadow: {
          primary: 'var(--color-shadow-sm)',
          secondary: 'var(--color-shadow-md)',
          tertiary: 'var(--color-shadow-lg)',
          extreme: 'var(--color-shadow-xl)',
        },

        // Status colors
        success: {
          DEFAULT: 'var(--color-success)',
          bg: 'var(--color-success-bg)',
        },
        warning: {
          DEFAULT: 'var(--color-warning)',
          bg: 'var(--color-warning-bg)',
        },
        error: {
          DEFAULT: 'var(--color-error)',
          bg: 'var(--color-error-bg)',
        },
      },

      // === Premium Card Extensions ===
      backdropBlur: {
        xs: '2px',
      },

      boxShadow: {
        'glow-blue': '0 0 20px rgba(59, 130, 246, 0.15)',
        'glow-purple': '0 0 20px rgba(147, 51, 234, 0.15)',
        'glow-green': '0 0 20px rgba(34, 197, 94, 0.15)',
        'glow-red': '0 0 20px rgba(239, 68, 68, 0.15)',
        'glow-orange': '0 0 20px rgba(249, 115, 22, 0.15)',
        'card-dark': '0 10px 25px rgba(0, 0, 0, 0.3)',
        'card-elevated': '0 20px 50px rgba(0, 0, 0, 0.4)',
        'glow-large': '0 0 40px rgba(59, 130, 246, 0.2)',
      },

      // === Background Images ===
      backgroundImage: {
        'gradient-radial': 'radial-gradient(var(--tw-gradient-stops))',
        'gradient-conic': 'conic-gradient(from 180deg at 50% 50%, var(--tw-gradient-stops))',
        'accent-glow': 'radial-gradient(circle at center, var(--color-accent-glow), transparent)',
      },

      // === Custom Animation ===
      animation: {
        'fade-in': 'fadeIn 0.2s ease-in-out',
        'slide-up': 'slideUp 0.3s ease-out',
        'pulse-glow': 'pulseGlow 2s cubic-bezier(0.4, 0, 0.6, 1) infinite',
        'float': 'float 3s ease-in-out infinite',
        'shimmer': 'shimmer 3s ease-in-out infinite',
        'border-glow': 'borderGlow 2s ease-in-out infinite',
        'slide-in-up': 'slideInUp 0.5s ease-out',
      },

      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { transform: 'translateY(10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        pulseGlow: {
          '0%, 100%': { boxShadow: '0 0 20px var(--color-accent-glow)' },
          '50%': { boxShadow: '0 0 30px var(--color-accent-glow)' },
        },
        float: {
          '0%, 100%': { transform: 'translateY(0px)' },
          '50%': { transform: 'translateY(-10px)' },
        },
        shimmer: {
          '0%': { transform: 'translateX(-100%)' },
          '100%': { transform: 'translateX(100%)' },
        },
        borderGlow: {
          '0%, 100%': {
            borderColor: 'rgba(59, 130, 246, 0.2)',
            boxShadow: '0 0 20px rgba(59, 130, 246, 0.1)'
          },
          '50%': {
            borderColor: 'rgba(59, 130, 246, 0.4)',
            boxShadow: '0 0 30px rgba(59, 130, 246, 0.2)'
          },
        },
        slideInUp: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },

      // === Transitions ===
      transitionProperty: {
        'colors': 'color, background-color, border-color, text-decoration-color, fill, stroke',
        'opacity': 'opacity',
        'shadow': 'box-shadow',
        'transform': 'transform',
      },

      transitionDuration: {
        'DEFAULT': '200ms',
        'fast': '150ms',
        'slow': '300ms',
      },

      transitionTimingFunction: {
        'DEFAULT': 'cubic-bezier(0.4, 0, 0.2, 1)',
        'in-out': 'cubic-bezier(0.4, 0, 0.2, 1)',
        'out': 'cubic-bezier(0, 0, 0.2, 1)',
        'in': 'cubic-bezier(0.4, 0, 1, 1)',
      },

      // === Spacing Extensions ===
      spacing: {
        '18': '4.5rem',
        '88': '22rem',
        '128': '32rem',
      },

      // === Border Radius Extensions ===
      borderRadius: {
        '4xl': '2rem',
      },

      // === Typography Extensions ===
      fontFamily: {
        sans: [
          '-apple-system',
          'BlinkMacSystemFont',
          '"Segoe UI"',
          'Roboto',
          '"Helvetica Neue"',
          'Arial',
          'sans-serif',
        ],
        mono: [
          '"SF Mono"',
          '"Monaco"',
          '"Inconsolata"',
          '"Roboto Mono"',
          '"Source Code Pro"',
          'monospace',
        ],
      },

      fontSize: {
        '2xs': ['0.625rem', { lineHeight: '0.75rem' }],
        '3xl': ['1.875rem', { lineHeight: '2.25rem' }],
        '4xl': ['2.25rem', { lineHeight: '2.5rem' }],
        '5xl': ['3rem', { lineHeight: '1' }],
        '6xl': ['3.75rem', { lineHeight: '1' }],
      },

      // === Z-Index Scale ===
      zIndex: {
        '60': '60',
        '70': '70',
        '80': '80',
        '90': '90',
        '100': '100',
      },
    },
  },
  plugins: [
    // Plugin for premium card utilities
    function({ addUtilities, theme }) {
      const newUtilities = {
        '.glass-effect': {
          'background': 'rgba(30, 41, 59, 0.3)',
          'backdrop-filter': 'blur(10px)',
          'border': '1px solid rgba(148, 163, 184, 0.1)',
        },
        '.glass-effect-strong': {
          'background': 'rgba(30, 41, 59, 0.5)',
          'backdrop-filter': 'blur(20px)',
          'border': '1px solid rgba(148, 163, 184, 0.2)',
        },
        '.bg-glow': {
          'box-shadow': `0 0 20px ${theme('colors.accent.glow')}`,
        },
        '.text-glow': {
          'text-shadow': `0 0 10px ${theme('colors.accent.primary')}`,
        },
        '.border-accent-subtle': {
          'border-color': theme('colors.border.light'),
        },
        '.glass-morphism': {
          'background': 'rgba(255, 255, 255, 0.05)',
          'backdrop-filter': 'blur(10px)',
          'border': `1px solid ${theme('colors.border.light')}`,
        },
        '.glass-dark': {
          'background': 'rgba(10, 14, 26, 0.8)',
          'backdrop-filter': 'blur(16px)',
          'border': `1px solid ${theme('colors.border.medium')}`,
        },
        '.hover-lift': {
          'transition': 'all 0.3s ease',
        },
        '.hover-lift:hover': {
          'transform': 'translateY(-4px)',
          'box-shadow': '0 20px 40px rgba(0, 0, 0, 0.3)',
        },
        '.text-gradient-blue': {
          'background': 'linear-gradient(to right, #3b82f6, #06b6d4)',
          '-webkit-background-clip': 'text',
          '-webkit-text-fill-color': 'transparent',
          'background-clip': 'text',
        },
        '.text-gradient-purple': {
          'background': 'linear-gradient(to right, #a855f7, #ec4899)',
          '-webkit-background-clip': 'text',
          '-webkit-text-fill-color': 'transparent',
          'background-clip': 'text',
        },
        '.text-gradient-green': {
          'background': 'linear-gradient(to right, #22c55e, #10b981)',
          '-webkit-background-clip': 'text',
          '-webkit-text-fill-color': 'transparent',
          'background-clip': 'text',
        },
        '.focus-ring': {
          '@apply focus:outline-none focus:ring-2 focus:ring-blue-500/50 focus:ring-offset-2 focus:ring-offset-slate-900': {},
        },
      }
      addUtilities(newUtilities)
    },
  ],
}